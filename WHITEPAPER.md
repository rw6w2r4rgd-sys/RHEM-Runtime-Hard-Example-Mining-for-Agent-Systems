# RHEM — Runtime Hard-Example Mining for Agent Systems

**版本：`v0.3.0`（2026-09-12）**

**Agent 级难例挖掘：让 Agent 从自己的运行错误里免重训地即时进化。**

训练界挖的是**模型的难例**；这个方法挖的是**整个 Agent 系统运行期的难例**——包含前者，还多出流程层与知识层两层。

## 一句话

> 让 Agent 像人一样“长教训”：错一次，教会它一类；免重训，当场生效；攒下的难例，喂给未来的大模型。

## 30 秒看懂定位

LLM 用 RLHF 从人类反馈里进化；**这套机制让 Agent 体系从自己的运行错误里进化**：

- 识别错误 → 进别名库；流程 bug → 结构性修复；规则缺失 → 自动开药方、人批准；知识缺口 → 补词条
- 有错误图时，BFS 圈定影响范围，DFS 沿上游关系寻找根因候选，同源症状先归并再门控
- 门控（同一图簇或同类错累计达标）后才并库，防单次误报投毒与同源症状重复放大
- `v0.2.0` 可选自适应门控：快速复发降档，长期安静维持基准，高危或复查失败锁高档并转人工
- `v0.3.0` 可选难例蒸馏：并库前分级，库内按命中、场景覆盖和失败反馈回炼，所有动作可回滚
- 护栏域（对错标准、硬约束）写死禁改——**进化的是记忆层，不是基因组，可回滚**
- 难例是“当下的守卫 + 未来的燃料”：Agent 就是**大模型的数据前哨**

## 2 分钟看懂机制

```
✗ 识别出错(现场/用户点纠错)
   ↓ 来源标记入待学习区
   ↓ 可选 BFS/DFS 图探针：先归并同源症状
   ↓ 图簇或同类错独立证据计数达标(门控)
   ↓ 可选蒸馏：高价值放行，中低价值继续留样
   ↓ 自动并库 → 下次命中
   ↕ 决策器开"药方" → 人工批准才生效
   所有自动变更走放射性补丁(快照+回滚+复扫)；可选库内回炼升核、降权或清除；护栏域运行时禁止改写
```

**四类难例**（覆盖 Agent 系统全类型错误）：

| 类 | 例子 | 去向 |
|---|---|---|
| 识别错误 | 槽位串位 / 歧义误判 | 别名库 |
| 流程错误 | 死锁 / 无限重试 | 结构性修复（不入库） |
| 规则缺失 | 某说法无规则管 | 决策器开药方 → 人批准 |
| 知识缺口 | 未知词 / 未收录品名 | 未识别上报 → 人归因 |

**与训练界难例挖掘的区别**：

| | 训练界 | RHEM |
|---|---|---|
| 对象 | 模型训练样本 | **运行期全类型错误** |
| 时机 | 训练阶段 | **运行期** |
| 生效 | 重训+发版（慢/贵） | **免重训，即时** |
| 成本 | 高 | 近零 |
| 节奏 | 批量 | **逐单回流** |

## 应用场景：FDE（前沿部署工程师）— Deployment is Training

> **每个部署点 = 一口难例矿，FDE = 矿工 + 确认者。**

现场环境全是开发期没见过的长尾数据。RHEM 让 FDE 在**当场**点✗纠正、当场回收、当场命中——不用“回总部-重训-发版-再验证”跑几周。多现场部署形成**分布式数据飞轮**：部署点越多 → 模型越强 → 现场返工越少。

**解放的是重复劳动，留下的是判断**——FDE 从“救火队”变成“种矿的人”，把省下的精力铺向更多现场。

## 业内对照

| 组件 | 出处 | 参考 |
|---|---|---|
| 规则优先、零 LLM | 通用实践 | — |
| 便宜模型优先、逐级兜底 | FrugalGPT (Stanford 2023) / RouteLLM (2024) | 成本最高降 98% |
| 监督喂养、周期合并 | Online / Active learning | HITL-NLP 闭环 |
| 错误回流喂未来 | Feedback loop / Data flywheel | HITL-NLP |
| **难例免重训迁移至运行期** | **本文（整合方法原创）** | 在可查范围内未见同形整合 |

## 诚实声明（局限）

1. 欠泛化——只复用见过的，靠“攒数据喂未来”补偿；
2. 缺评估回路（最薄弱）——需补 benchmark / 回归测试；
3. 默认门控仍靠固定计数；`v0.2.0` 已提供可选自适应试点，但尚未完成评估集与冲突验证；
4. 置信度信号未定义——LLM 自报可靠度需校准；
5. 规则库长尾膨胀——`v0.3.0` 已提供可选蒸馏与回滚治理，但仍需真实长尾验证和人工维护纪律。

## Roadmap

- [x] Now：纠错反馈 → 带来源标注的待学习区；BFS/DFS 图探针；图簇或同类独立证据计数达标并库；决策器开药方人工批准；自动变更全走补丁治理；护栏域禁止运行时变更
- [x] v0.2.0：固定门控保持默认；提供可选快速复发降档、高危锁定和门控审计
- [x] v0.3.0：提供可选并库前分馏、反馈账本、库内回炼、补丁与回滚
- [ ] Future：难例攒成带标注数据集 → 喂模型重训换泛化；建立离线评估回路；闭环“大模型数据前哨”

## 许可与仓库边界

方法论与 MIT 参考实现在本仓库公开；生产环境中的防篡改、探针布置与私有脚本等实现细节不在本仓库。

## 引用

```bibtex
@misc{rhem2026,
  title={Runtime Hard-Example Mining for Agent Systems (RHEM)},
  author={徐应生},
  year={2026},
  howpublished={GitHub repository},
  note={First release: 2026-09-10}
}
```