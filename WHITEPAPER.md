# RHEM — Agent 级难例挖掘白皮书

**Runtime Hard-Example Mining for Agent Systems**

- 版本：`v0.4.0`（2026-09-12）
- 日期：2026-09-10 首发 / 2026-09-12 更新
- 定位：核心发明点论述 / 可验证首发底稿
- 作者：徐应生
- 仓库：`RHEM-Runtime-Hard-Example-Mining-for-Agent-Systems`

---

## 一句话定位

> **训练界挖“模型”的难例，RHEM 挖“整个 Agent 体系”的难例。后者包含前者，还多出流程层与知识层。**
>
> 难例守住当下，样本沉淀给未来：当前免重训更新系统记忆，长期把真实错误积累成模型可利用的数据资产。

这里能证明的，是参考机制可以在测试与演示中运行；不能据此声称生产准确率、成本收益或泛化能力已经得到验证。

---

## 0. 缘起：为什么识别这么难

实际运行中的典型现象：

1. 商品名被识别成商户名，实体发生双重身份歧义；
2. 商户名没有识别出来，因为开放集词表不可能一次收全；
3. 订单日期被识别成商品，发生槽位串位；
4. 本条没有录入后流程来回横跳，问题来自流程控制而不是识别。

根因不能混在一起处理：

| 现象 | 根因 | 性质 |
|---|---|---|
| 商品名 → 商户名 | 词有双重身份，缺少可靠上下文权重 | 识别歧义 |
| 商户名漏识别 | 开放集未登录，没进词表就不认识 | 知识缺口 |
| 日期 → 商品 | 槽位标签缺少强格式锚点 | 槽位串位 |
| 不录后卡死横跳 | 重试无止损线、无转人出口 | 流程缺陷 |

人类与机器的默认能力并不相同：

| 人通常具备 | 机器默认没有 |
|---|---|
| 母语词库与领域常识 | 只认识已提供的词表 |
| 对话上下文 | 口语上下文进入模型的信号往往稀薄 |
| 日期、数量等格式常识 | 需要显式规则或锚点 |
| 猜错后的自我纠正 | 猜错后可能继续沿错误路径执行 |

> 识别本质上是在非结构化口语里给槽位赋值。人类靠语言、上下文和领域记忆完成；机器则需要词库、锚点、规则和人工兜底。

一组常见但并非普适的工程启发式是：

1. 日期、数量等高格式字段优先使用确定性校验或正则锚点；
2. 商户名、商品名等开放集优先走词库命中，而非纯概率猜测；
3. 重试必须设置止损线，并把无法自动处理的情况转给人工。

---

## 1. 基石：规则 Agent 与模型 Agent

Agent 的经典定义是：

```text
感知 → 决策 → 行动
```

决策器并不必须是 LLM。使用确定性规则的决策器仍然构成 Agent，只是能力边界不同。

| 类型 | 决策器 | 特性与边界 |
|---|---|---|
| 模型 Agent | LLM 或概率模型 | 泛化潜力更强，但会失败、可降级、可能跳过步骤 |
| 规则 Agent | 确定性规则 | 在规则实现正确且输入命中的范围内结果可复现，不包含模型推理 |

“规则 Agent 永远对”不是严谨表述。更准确的边界是：

- 规则本身可能写错；
- 输入可能落在规则覆盖范围之外；
- 外部依赖可能失败；
- 因此规则层需要审核、版本、审计与回滚。

RHEM 采用三条设计红线：

1. 系统形态可以 Agent 化，但护栏内核不依赖模型概率；
2. 参考实现只做运行期学习闭环，不冒充完整生产平台；
3. 会想的模型层可失败，确定性规则层负责约束、门控与审计。

### 全系统身份示意

```text
大脑    = 模型 Agent
          负责理解与生成，可失败，可降级

脊柱    = 规则 Agent
          负责确定性约束、门控与护栏校验

观测仓  = 观测组件
          负责探针、证据与来源记录

决策芯  = 触发与决策组件
          错误或异常达到门控后才启动裁决

人工    = 最终批准者
          负责规则、知识、结构和高风险变更
```

---

## 2. 能力层：功能模块 Skill 化

Skill 是能力契约，热插拔是实现方式。先把接口稳定下来，替换内部实现时才不需要修改调用方。

一个 Skill 卡至少包含六项：

1. 名字；
2. 描述；
3. 输入契约；
4. 输出契约；
5. 权限；
6. 审计信息。

Skill 化的价值：

- 调用方只依赖 Skill 名和稳定契约；
- 允许 Skill 调用 Skill，形成可编排流程；
- 观测探针可挂在同一能力契约上；
- 权限可以拆分到最小范围；
- 新能力主要通过注册接入，而不是到处修改调用点。

但不是所有东西都应该热插拔：

| 组件 | 是否热插拔 | 原因 |
|---|---|---|
| 普通业务能力 | 可以 | 契约稳定后允许替换实现 |
| 状态型流程 | 谨慎 | 必须定义事务边界与恢复方案 |
| 护栏与对错标准 | 不可以 | 运行时自改红线等于移除约束 |
| 模型权重与系统提示词 | 不可以 | 不属于 RHEM 的运行时记忆层 |

结论是：该锁死的锁死，该替换的替换。契约提供稳定性，稳定之后才谈自由替换。

---

## 3. 进化机制：Agent 自进化

一句话概括：

> 人纠正一次错误，系统把这次纠正沉淀为下一次可直接使用的记忆。

### 3.1 监督信号

已有的人机交互信号可以拆成三类：

```text
✓ 确认 = 正样本：当前映射是正确的
✗ 纠正 = 错误到正确的映射
↻ 重输 = 输入本身有问题，不应直接作为学习样本
```

### 3.2 进化分层

| 层级 | 典型内容 | 能否自动处理 |
|---|---|---|
| 可成长记忆 | 别名、规则、词条、流程参数 | 达到门控后可以自动或受控更新 |
| 监督标准 | 什么算对、什么算错 | 必须由人或评估系统提供 |
| 结构问题 | 流程缺陷、不可达路径、错误约束 | 需要人工审核或专门修复 |
| 护栏域 | 对错标准、冻结硬约束 | 运行时禁止改写 |
| 模型权重 | 大模型参数 | 不属于本仓库的运行期路径 |

### 3.3 进化闭环

```text
识别错误
  → 反馈进入待学习区，保留来源和证据
  → 同族或同图簇独立证据累计
  → 门控判断
  → 可选蒸馏判断记录价值
  → 别名、规则、知识或结构补丁
  → 人工批准高风险变更
  → 历史难例复扫
  → 可选回炼与蒸馏反哺
```

### 3.4 保险丝

任何自动变更都必须具备：

- 来源与证据；
- before / after 快照；
- 补丁顺序；
- 回滚能力；
- 复扫结果；
- 审计日志。

越能自动修改，越必须能够撤回。

---

## 4. 核心发明点：Agent 级难例挖掘

### 4.1 定义

> 将难例的范围从“模型训练样本”扩展到 **Agent 运行期产生的全类型错误**：识别错误、流程缺陷、规则缺失和知识缺口。通过来源标记、待学习区、证据门控、可选蒸馏、补丁治理和人工批准，形成免重训的系统记忆更新闭环。

这里的“免重训”是指更新运行期记忆和配置，不修改模型权重，也不保证模型能力本身因此提升。

### 4.2 与训练界难例挖掘的区别

| 维度 | 训练界难例挖掘 | RHEM |
|---|---|---|
| 对象 | 模型训练样本 | Agent 运行期全类型错误 |
| 位置 | 训练阶段 | 任务运行期 |
| 生效 | 重训与部署 | 记忆层即时生效或生成待批准补丁 |
| 节奏 | 批次训练 | 逐单回流、累计门控 |
| 产物 | 更难样本集 | 别名、规则、词条、结构修复、审计记录 |
| 泛化 | 直接服务模型训练 | 当前主要复现已见修复，长期沉淀数据 |

训练界方法覆盖“模型分类错误”；RHEM 额外覆盖流程层和知识层，但不能因此声称训练界方法已经被替代。

### 4.3 四类难例

| 类别 | 示例 | 出口 |
|---|---|---|
| 识别错误 | 槽位串位、别名误判、意图混淆 | 别名库 |
| 流程错误 | 死锁、无限重试、错误重试风暴 | 结构性配置补丁 |
| 规则缺失 | 某类输入没有规则约束 | 药方 → 人工批准 → 规则库 |
| 知识缺口 | 未收录词、品名、SKU、术语 | 上报 → 人工归因 → 词条库 |

### 4.4 速成与泛化

| | 运行期难例挖掘 | 模型训练与泛化 |
|---|---|---|
| 驱动 | 实例、记忆、现场修正 | 参数、梯度、分布学习 |
| 优点 | 即时、可解释、免重训、逐单回流 | 能泛化到部分未见情况 |
| 弱点 | 主要复用见过的修复 | 冷启动、成本高、更新慢 |
| 互补 | 守住当下并生产真实错误数据 | 从积累数据中争取更广泛化 |

```text
运行期：错误 → 门控 → 记忆更新 → 同类问题即时处理
   ↓
沉淀期：高价值难例 → 带标注数据资产
   ↓
未来：重训或微调模型，争取更广泛化
```

> 难例既是当下的守卫，也是未来的数据燃料。

### 4.5 方法定位

> RHEM 是一个“运行期即时守卫 + 潜在数据前哨”的参考实验：当前解决同类错误快速回流，长期讨论真实错误如何沉淀成模型可利用的数据资产。

“数据前哨”是方向性定位。是否真能提升未来模型效果，需要后续评估集、数据质量和训练实验验证。

---

## 5. 业内对照与原创边界

| RHEM 构件 | 业内已有方案 | 参考 |
|---|---|---|
| 规则优先、模型兜底 | Rules-first / Cascade / Model routing | 通用工程实践 |
| 成本分层与路由 | FrugalGPT、RouteLLM | Stanford 2023 / 2024 |
| 人机反馈持续学习 | Online learning、Active learning、HITL | HITL-NLP 闭环 |
| 错误回流形成飞轮 | Feedback loop、Data flywheel | 通用机器学习实践 |
| 难例挖掘 | OHEM、Hard Negative Mining | 训练期难例方法 |
| 运行期全类型难例整合 | 本方法 | 在可查范围内未见同形整合 |

原创边界应这样表述：

- OHEM、难例挖掘、数据飞轮、级联路由、HITL 都不是本方法首次提出；
- RHEM 的主张是把这些成熟构件组合到 Agent 运行期，并扩展到流程层和知识层；
- “在可查范围内未见同形整合”是检索范围内的谨慎表述，不等于“绝对没人做过”。

---

## 6. 已知局限

1. **欠泛化**：主要复用见过的修复。`v0.4.0` 的蒸馏反哺只提供候选归纳入口，不等于已经解决泛化。
2. **缺评估回路**：这是当前最薄弱的部分。没有 benchmark、回归集和冲突检测，就无法可靠判断进化是否真的有效。
3. **门控仍以账本信号为主**：自适应门控可以调整收集快慢，但没有完成 F1、局部熵或评估集冲突度的验证。
4. **置信度校准未定义**：LLM 自报置信度不能直接当作可靠门控信号。
5. **规则库存在长尾膨胀风险**：`v0.3.0` 的蒸馏和回滚提供治理手段，但仍需要真实长尾验证与维护纪律。
6. **没有生产效果数据**：当前仓库没有线上 A/B、准确率提升、成本下降或稳定性收益的证明。
7. **并发与分布式能力有限**：参考实现是单进程 JSON 记忆库，不是高并发生产数据库。

把这些局限写清楚，是为了让机制可检验，而不是靠口号替代证据。

---

## 7. 进化动力系统：四组件与当前状态

四组件分别处理收集、纯度、反哺和复查。当前完成度不同，不能把 Roadmap 当成既有功能。

| 组件 | 类比 | 负责 | 当前状态 |
|---|---|---|---|
| 再生反馈环 | 方向盘 | 复查修复是否仍有效 | 已有反馈账本和 `rescan` 基线；完整自动闭环仍待验证 |
| 自适应门控 | 油门 | 调整收集速度与风险锁 | `v0.2.0` 可选实现 |
| 难例蒸馏 | 精炼塔 | 判断记录价值、升核、降权、清除 | `v0.3.0` 可选实现 |
| 蒸馏反哺 | 炼油炉 | 从高纯记录归纳规则、结构和隐患候选 | `v0.4.0` 可选实现，人工批准后生效 |

### 7.1 二阶进化闭环

```text
错误
  → 回收与来源标记
  → 可选蒸馏：先判断记录价值
  → 门控：独立证据达标才继续
  → 并库或生成提案
  → 反馈账本与复扫：检查修复是否有效
  → 可选蒸馏反哺：从 core/high 账本归纳候选
  → 人工批准
  → 下一轮收集与评估
```

第一轮是“错一次学一条”；第二轮尝试“多炉高纯样本归纳一片”。这仍不等同于模型训练，而是运行期记忆层的受控归纳。

### 7.2 共享护栏总纲

1. 只调整“收快慢、留哪些、归纳什么候选”，不改对错标准和护栏域；
2. 自动变化必须进入补丁治理；
3. 蒸馏和反哺只使用账本字段，不引入 LLM 主观补全；
4. 规则模板、结构和隐患候选必须人工批准；
5. 回滚与复扫必须可用；
6. 没有评估数据时，不声明生产收益。

### 7.3 会师论

难例发掘在训练场里改模型；RHEM 在运行现场改记忆。两者终点接近，但路基不同。

| | 训练界难例发掘 | RHEM |
|---|---|---|
| 原料 | 训练集难样本 | 运行期全类型错误 |
| 主要机制 | 权重、梯度、重训 | 人工批准的规则合并、别名、词条与结构补丁 |
| 时机 | 训练阶段 | 运行期持续 |
| 直接产物 | 更准的模型 | 更准的运行记忆与审计数据 |
| 关系 | 追求更广泛化 | 守住当下并沉淀数据 |

这不是撞车，而是会师：RHEM 积累的高纯难例和规则候选，未来可能成为模型训练的数据输入；但能否转化为泛化能力，仍需训练与评估验证。

更详细的先后关系见 [先驱与绘图师](docs/PIONEER-AND-CARTOGRAPHER.md)。

---

## 8. 落地与未来

### 8.1 当前参考实现已完成

- [x] 带来源和证据的待学习区；
- [x] BFS/DFS 图探针与图簇门控；
- [x] 固定门控；
- [x] 可选自适应门控 `v0.2.0`；
- [x] 四类难例出口；
- [x] 规则和知识的人工批准；
- [x] before / after 快照、顺序回滚与复扫；
- [x] 护栏域运行时禁写；
- [x] 可选难例蒸馏 `v0.3.0`；
- [x] 可选蒸馏反哺 `v0.4.0`；
- [x] 42 项单元测试和离线演示。

### 8.2 尚未完成

- [ ] 接入真实 Agent 轨迹和可靠验证器；
- [ ] 建立离线评估集、回归集和冲突检测；
- [ ] 验证自适应门控在真实长尾上的收益与风险；
- [ ] 验证蒸馏和反哺是否真的降低重复错误或维护成本；
- [ ] 把高纯难例沉淀成带标注数据资产；
- [ ] 通过重训或微调验证数据对模型泛化的价值；
- [ ] 生产级持久化、并发、权限和分布式审计。

---

## 9. 可验证首发声明与口径

可以验证的内容：

- 本仓库提交历史、标签和 Release 时间；
- Zenodo 概念与版本归档记录；
- 四类难例、图探针、门控、蒸馏、反哺、补丁和回滚测试；
- 每个版本对应的不可变代码归档。

对外口径：

> 在可查范围内，未见与本项目相同形态的“Agent 运行期全类型难例挖掘 + 门控 + 蒸馏 + 受控反哺”整合。

不应写成：

- “颠覆难例挖掘”；
- “绝对没人做过”；
- “已经解决泛化”；
- “生产准确率或成本已经提升”。

首发证据属于代码与归档，不属于口头声明。机制是否有效，仍应交给复现、反例和未来评估。

---

## 10. 许可、仓库边界与引用

方法论与 MIT 参考实现在本仓库公开。生产环境中的防篡改、私有探针、内部部署脚本和未公开验证材料不在本仓库。

- [README](README.md)
- [难例蒸馏](docs/DISTILLATION.md)
- [蒸馏反哺](docs/INDUCTION.md)
- [自适应门控](docs/ADAPTIVE-GATING.md)
- [双阶段图探针](docs/GRAPH-PROBE.md)
- [先驱与绘图师](docs/PIONEER-AND-CARTOGRAPHER.md)

### 引用

```bibtex
@misc{rhem2026,
  title={Runtime Hard-Example Mining for Agent Systems (RHEM)},
  author={徐应生},
  year={2026},
  version={0.4.0},
  doi={10.5281/zenodo.22719563},
  howpublished={GitHub repository},
  note={First release: 2026-09-10}
}
```
